// eslint-disable-next-line react/prop-types
function Button({name}) {

    return (
        // eslint-disable-next-line react/prop-types
        <button>{ name }</button>
    )
}
export default Button;
